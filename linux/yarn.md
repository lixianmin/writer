



```shell

# 不要使用以下方式安装, 因为版本问题, 事后又卸载了
sudo apt install nodejs
sudo apt install npm

sudo apt uninstall nodejs
sudo apt uninstall npm

npm install -g yarn
npm uninstall -g yarn

#####################################################
yarn install 		# 安装依赖包
yarn run build		# 打包

```

